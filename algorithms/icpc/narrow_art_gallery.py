def narrow_art_gallery():
    first = input().split()
    rows = int(first[0])
    k = int(first[1])
    sol = []
    gallery = [] * rows
    for i in range(rows):
        var = input().split()
        gallery.append([int(var[0]), int(var[1])])

    print(gallery)    
    index = 1

    def dom_loves_nested():
        
    # while gal[index] != '0 0':
    
    
    # return

    # narrow_art_gallery(row, k, sol)

narrow_art_gallery()